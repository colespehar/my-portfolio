# 🧭 Git Workflow Guide

## Always on main
git add .
git commit -m "Some change"
git push origin main

## Then deploy
npm run deploy-safe

npx rimraf node_modules dist .vite && npm install && npm run dev